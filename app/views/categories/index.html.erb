<h1>Categories</h1>
<% @category.each do |category| %>
<p>
  <%= category.name %>
  <%= link_to 'Edit', edit_category_path(category) %>
  <%= link_to 'Delete', category_path(category), method: :Delete, datea:{confirm: 'Are you certain???'}%>
</p>
<% end %>
<%= link_to "Add New",new_category_path %>
